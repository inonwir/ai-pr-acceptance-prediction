## Code and Data Availability

The dataset tables used in this study are drawn from the public AIDev release. Our analysis uses a custom filtered subset of these public tables, rather than the original AIDev-pop subset reported in the dataset paper. The full replication package, including preprocessing, feature engineering, model evaluation, ablation, and figure-generation scripts, is available at **[GitHub URL]**. A versioned archive corresponding to this submission will be preserved at **[Zenodo DOI or GitHub release URL]**.

## Shorter alternative

The code used for preprocessing, feature engineering, model evaluation, and figure generation is available at **[GitHub URL]**. We use public AIDev tables but define our own analysis subset rather than reusing the original AIDev-pop subset.
